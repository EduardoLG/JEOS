// Reserved chrome heights shared by the window/icon layout math.
// Must stay in sync with WindowManager's container insets (top-8 / bottom-20).
export const MENU_BAR_HEIGHT = 32
export const DOCK_RESERVED_HEIGHT = 80
